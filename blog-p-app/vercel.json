{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {}
    }
  ],
  "scripts": {
    "build": "tsc -b && vite build"
  },
  "outputDirectory": "dist"
}
